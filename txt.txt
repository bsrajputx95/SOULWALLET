React Native Specific Audit
1. Performance Analysis
bash
# Bundle analysis
npx react-native bundle --platform android --dev false --entry-file index.js --bundle-output android.bundle --sourcemap-output android.sourcemap
npx react-native bundle --platform ios --dev false --entry-file index.js --bundle-output ios.bundle --sourcemap-output ios.sourcemap

# Check bundle size
ls -lah *.bundle

# Metro bundler analysis
npx metro-bundler --profile
2. React Native Specific Metrics
✅ App Size (APK/IPA under 30MB)

✅ Startup Time (< 2 seconds)

✅ FPS (consistent 60 FPS)

✅ Memory Usage (< 100MB in most cases)

🛠 React Native Optimization Areas
A. Bundle Optimization
typescript
// Check for lazy loading
const HeavyScreen = React.lazy(() => import('./HeavyScreen'));

// Import only what you need
import { View } from 'react-native'; // ✅ Good
import * as ReactNative from 'react-native'; // ❌ Bad
B. Image Optimization
typescript
// Are you using optimized images?
import FastImage from 'react-native-fast-image'; // Better performance

// Check image formats and sizes
// Use WEBP format for smaller bundle
C. State Management & Re-renders
typescript
// Use React.memo for expensive components
const ExpensiveComponent = React.memo(({ data }) => {
  return <Text>{data}</Text>;
});

// Use useCallback/useMemo appropriately
D. Navigation Performance
typescript
// Check if you're using efficient navigation
import { createNativeStackNavigator } from '@react-navigation/native-stack'; // ✅ Better performance
// vs
import { createStackNavigator } from '@react-navigation/stack'; // ❌ Slower
📊 React Native Industry Standards Checklist
✅ Performance
Hermes engine enabled

Bundle splitting for multiple platforms

Optimized images (WEBP format)

FlatList for long lists with proper keyExtractor

Avoid inline functions in render

✅ TypeScript Quality
Proper type definitions for components

Type-safe navigation

Strict mode enabled in tsconfig.json

No any types

✅ Code Structure
Clean separation of concerns

Custom hooks for business logic

Proper error boundaries

Consistent naming conventions

✅ Developer Experience
ESLint + Prettier for React Native

Husky for pre-commit hooks

Proper debugging setup (Flipper/Reactotron)

🚀 Quick React Native Optimization Commands
bash
# Check TypeScript issues
npx tsc --noEmit

# Analyze bundle
npx react-native-bundle-visualizer

# Check for outdated dependencies
npx npm-check-updates

# Audit security issues
npm audit
🔧 Specific Tools for React Native
Performance Monitoring:
bash
# React DevTools for profiling
npx react-devtools

# Flipper for advanced debugging
# Install Flipper desktop app
Bundle Analysis:
Use react-native-bundle-visualizer

Check metro.config.js for optimization options

📱 Deployment Optimization
Before Building APK/IPA:
Enable ProGuard/R8 for Android

Enable tree shaking

Remove console.logs in production

Optimize images with react-native-optimize

Use app icons of correct sizes